#include<stdio.h>
#include<conio.h>
int factorial(int);
int main()
{
	int fact,num;
	printf("\n enter any integer number:");
	scanf("%d",&num);
	fact=factorial(num);
	printf("\n factorial of %d is %d",num,fact);
}
int factorial(int n)
{
	if(n==0)
	return(1);
	else
	return(n*factorial(n-1));	
}
